# Screenshots

Drop PNGs here and uncomment the table in the top-level README:

- `panel.png`   — ops dashboard / deliveries grid
- `proof.png`   — proof detail (photos + signature + map)
- `mobile.png`  — MAUI driver app (delivery list or capture form)
